# Fuzz Targets

Parser and hostile-input fuzz targets live here. The current targets are scaffold placeholders; they deliberately avoid consensus behavior until hsd fixture tests define parser expectations.

Run targets with `cargo fuzz run <target>` after installing `cargo-fuzz`.
