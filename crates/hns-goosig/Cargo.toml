[package]
name = "hns-goosig"
version.workspace = true
edition.workspace = true
publish.workspace = true
build = "build.rs"
license = "MIT AND Apache-2.0 AND BSD-3-Clause AND (LGPL-3.0-or-later OR GPL-2.0-or-later)"
description = "Safe verification-only wrapper around the pinned Goosig implementation used by HSD"

[build-dependencies]
cc = "1.2"

[dependencies]
thiserror.workspace = true
